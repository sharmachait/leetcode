export default function LeaderBoardList({
  list,
}: {
  list: { image: string; name: string; points: string }[];
}) {
  return (
    <div>
      Leader board list Leader board listLeader board listLeader board
      listLeader board listLeader board listLeader board listLeader board
      listLeader board listLeader board listLeader board listLeader board
      listLeader board listLeader board list
    </div>
  );
}
